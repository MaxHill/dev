{
  "mcp": {
    "playwright": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@playwright/mcp",
        "--executable-path",
        "/usr/bin/chromium"
      ],
      "enabled": true,
      "timeout": 30000
    }
  }
}
