# Input configuration overrides for keyboard layouts
# Adds Swedish keyboard layout with Left Alt + Right Alt toggle

input {
  # Use multiple keyboard layouts and switch between them with Left Alt + Right Alt
  kb_layout = us,se
  kb_options = compose:caps,grp:alts_toggle
}
